version: 0.0
os: linux

files:
  - source:  /
    destination: /home/ubuntu/app
    overwrite: yes

permissions:
  - object: /home/ubuntu/app/
    owner: ubuntu
    group: ubuntu
    mode: 755

hooks:
  AfterInstall:
    - location: ./start.sh
      timeout: 60
      runas: ubuntu
#    - location: ./.sh
#      timeout: 60
#      runas: ubuntu
#  ApplicationStart:
#    - location: ./start.sh
#      timeout: 60
#      runas: ubuntu